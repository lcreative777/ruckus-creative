---
// src/components/Footer.astro — Cinema footer.
// The handoff's footer drops the address/phone/email block the light
// build showed — that identity content already lives in the homepage
// contact section and the header's mobile panel, so this stays to logo +
// copyright + the sidebar/legal links.
import { Image } from 'astro:assets';
import { site, sidebarLinks, legalLinks } from '../data/site';
import logoLight from '../assets/chrome/ruckus_logo_light.png';

const footerLinks = [...sidebarLinks, ...legalLinks];

/**
 * Photo credit for a page's header image, surfaced here rather than over the
 * image itself. Optional — only the content pages carry a photographic band.
 */
interface Props { photoCredit?: { photographer: string; photographerUrl: string } }
const { photoCredit } = Astro.props;
---
<footer class="site-footer">
  <div class="container-wide bar">
    <div class="brand">
      <Image src={logoLight} alt={site.name} height={30} widths={[143, 286, 429]} sizes="143px" loading="lazy" />
      <p>© {site.legalName}</p>
    </div>
    <nav aria-label="Footer">
      <ul>
        {footerLinks.map(l => <li><a href={l.href}>{l.label}</a></li>)}
      </ul>
    </nav>
  </div>
  {photoCredit && (

    <p class="photo-credit">

      Photo by <a href={photoCredit.photographerUrl} rel="noopener nofollow" target="_blank">{photoCredit.photographer}</a>

    </p>

  )}

</footer>

<style>
  .site-footer {
    background: var(--color-footer);
    border-top: 1px solid rgba(255, 255, 255, .09);
    padding-block: var(--space-4);
  }
  .bar {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
  }
  .brand { display: flex; align-items: center; gap: var(--space-2); }
  .brand :global(img) { height: 30px; width: auto; opacity: .85; }
  .brand p {
    margin: 0;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 12px;
    letter-spacing: .04em;
    color: var(--color-text-muted);
  }
  nav ul { list-style: none; margin: 0; padding: 0; display: flex; flex-wrap: wrap; gap: var(--space-3); }
  nav a {
    text-decoration: none;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 11px;
    letter-spacing: .18em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    transition: color .2s ease;
  }
  nav a:hover { color: #fff; }
  .photo-credit {
    margin: var(--space-3) 0 0;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 10px;
    letter-spacing: .12em;
    text-transform: uppercase;
    color: var(--color-text-dim, rgba(255,255,255,.35));
  }
  .photo-credit a { color: inherit; text-decoration: none; }
  .photo-credit a:hover { color: #fff; text-decoration: underline; }
</style>
